{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Python in Correct Folder",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}"
        }
    ]
}